let adicao = (a,b,c) => a + b + c;

console.log(adicao(5,5,5));

let alerta = (nome) => `Ultimo alerta para ${nome}`

console.log(alerta("João"));

let saudar = (nome) => {
    let saudacao = "Bom dia, ";
    return `${saudacao}${nome}!`;
}

console.log(saudar("Ana"));